[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5

# Add a second Railway service pointed at this same repo with:
#   startCommand = "python -m agent.orchestrator.run_scheduler"
# for the background worker (sending, reply polling, weekly reports).
